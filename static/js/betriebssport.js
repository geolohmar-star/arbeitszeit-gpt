/* Betriebssport – seitenspezifische Logik.
   HTMX-CSRF wird global in base.html gesetzt (htmx:configRequest).
   Diese Datei bleibt als Erweiterungspunkt fuer kuenftige Anforderungen. */
document.addEventListener("DOMContentLoaded", function () {
    /* Tooltip fuer Spaltenkoepfe (Name) per title-Attribut – kein zusaetzlicher Code noetig. */
});
